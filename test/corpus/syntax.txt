========
Notation
========

notation " ♔ " => Foo.bar

---

(module
  (notation
    (string)
    (field_of (identifier) (identifier))))

===========
Macro Rules
===========

macro_rules
  | `(tactic| foo) => `(tactic|trivial)
  | `(tactic| bar) => `(tactic|rfl)

---

(module
  (macro_rules
    (pattern
      (quoted_tactic (identifier))
      (quoted_tactic (identifier)))
    (pattern
      (quoted_tactic (identifier))
      (quoted_tactic (identifier)))))

======
Syntax
======

syntax "foo" : tactic
syntax str : tactic

---

(module
  (syntax (string) (identifier))
  (syntax (identifier) (identifier)))
