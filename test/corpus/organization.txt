===============
Empty Namespace
===============

namespace Foo
end Foo

---

(module (namespace (identifier) (identifier)))

=============
Empty Section
=============

section Foo
end Foo

---

(module (section (identifier) (identifier)))

======================
Nested Empty Namespace
======================

namespace Foo
namespace Bar
end Bar
end Foo

---

(module
  (namespace (identifier)
    (namespace (identifier) (identifier)) (identifier)))

====================
Nested Empty Section
====================

section Foo
section Bar
end Bar
end Foo

---

(module
  (section (identifier)
    (section (identifier) (identifier)) (identifier)))

==========================
Namespace With Definitions
==========================

namespace Foo

theorem foo : p := sorry
theorem bar : q := sorry

end Foo

---

(module
  (namespace (identifier)
    (theorem (identifier) (identifier) (identifier))
    (theorem (identifier) (identifier) (identifier)) (identifier)))

========================
Section With Definitions
========================

section Foo

theorem foo : p := sorry
theorem bar : q := sorry

end Foo

---

(module
  (section (identifier)
    (theorem (identifier) (identifier) (identifier))
    (theorem (identifier) (identifier) (identifier)) (identifier)))

=======================
Namespace With Sections
=======================

namespace Foo

section Bar

theorem bar : q := sorry

end Bar

section Baz

theorem baz : r := sorry

end Baz

end Foo

---

(module
  (namespace (identifier)
    (section (identifier)
      (theorem (identifier) (identifier) (identifier)) (identifier))
    (section (identifier)
      (theorem (identifier) (identifier) (identifier)) (identifier))
    (identifier)))
